import{completeQuest,parseCompleteQuestResult}from'./completion-repository';const wire={status:'completed',outcome:'completed',completion_id:'c1',instance_id:'q1',xp_awarded:150,total_xp:150,level_before:1,level_after:2,completed_count:1,completed_at:'2026-08-31T10:00:00Z'}as const;
test('SQ-0503 calls authoritative completion and parses immutable totals',async()=>{const rpc=jest.fn(async()=>({data:wire,error:null}));await expect(completeQuest({rpc}as never,'q1','k1')).resolves.toMatchObject({xpAwarded:150,totalXp:150,levelAfter:2});expect(rpc).toHaveBeenCalledWith('complete_quest',{p_quest_instance_id:'q1',p_idempotency_key:'k1'});});
test('SQ-0503 parses replay and rejects malformed authority output',()=>{expect(parseCompleteQuestResult({...wire,outcome:'already_completed'})).toMatchObject({outcome:'already_completed'});expect(()=>parseCompleteQuestResult({...wire,total_xp:-1})).toThrow('Invalid completion response');});
test('SQ-0503 surfaces RPC failure',async()=>{const error=new Error('offline');await expect(completeQuest({rpc:jest.fn(async()=>({data:null,error}))}as never,'q','k')).rejects.toBe(error);});
